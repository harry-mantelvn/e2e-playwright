{
  "name": "automationexercise-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:browsers": "npx playwright install --with-deps",
    "clean": "node clean-reports.js",
    "test:headed": "npm run clean && cross-env NODE_ENV=test npx playwright test --headed",
    "test:smoke": "npm run clean && cross-env NODE_ENV=test npx playwright test tests/smoke && npm run allure:generate",
    "test:regression": "npm run clean && cross-env NODE_ENV=prerelease npx playwright test tests/regression && npm run allure:generate",
    "test:all": "npm run clean && cross-env NODE_ENV=test npx playwright test && npm run allure:generate",
    "report:html": "npx playwright show-report",
    "allure:clean": "npm run clean",
    "allure:generate": "npx allure generate --single-file -c ./test-reports/allure-results --clean -o single-file-reports",
    "allure:serve": "npx allure serve test-reports/allure-results",
    "auth:setup": "cross-env NODE_ENV=test npx playwright test helper/authentication-setup.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "allure-playwright": "^2.14.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@playwright/test": "^1.55.1"
  }
}
